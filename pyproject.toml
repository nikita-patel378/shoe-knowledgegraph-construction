[project]
name = "shoe-knowledgegraph-construction"
version = "0.1.0"
description = "Extract information with BAML and create knowledge graph"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "baml-py==0.211.2",
    "dotenv>=0.9.9",
]
